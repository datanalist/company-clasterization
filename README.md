# Система кластеризации компаний по новостным данным

Данная система предназначена для автоматического объединения компаний в кластеры на основе новостных данных. Система анализирует новостные источники, выявляет упоминания компаний и группирует их по контекстуальной близости.

## Архитектура системы

- **Frontend:** Streamlit
- **Backend:** FastAPI
- **База данных:** SQLite
- **Модели машинного обучения:** Sentence Transformers, UMAP, HDBSCAN

## Функциональность

- Автоматическое объединение компаний в кластеры на основе новостных данных
- Присваивание каждому кластеру информативного названия
- Возможность выбора периода новостного охвата (день, неделя, месяц или произвольный период)
- Визуализация кластеров компаний
- Система пользователей с биллингом (кредитная система)

## Установка и запуск

### Предварительные требования

- Python 3.8+
- Pip

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка переменных окружения

Создайте файл `.env` в корне проекта и добавьте следующие переменные:

```
SECRET_KEY=your_secret_key
TELEGRAM_API_ID=your_telegram_api_id
TELEGRAM_API_HASH=your_telegram_api_hash
TELEGRAM_PHONE=your_telegram_phone
TELEGRAM_USERNAME=your_telegram_username
```

### Запуск backend (FastAPI)

```bash
cd src
uvicorn app:app --reload
```

### Запуск frontend (Streamlit)

```bash
cd src/frontend
streamlit run app.py
```

## Использование системы

1. Зарегистрируйтесь в системе или войдите в существующий аккаунт
2. Выберите период для анализа новостей
3. Запустите процесс кластеризации
4. Дождитесь завершения процесса
5. Просмотрите результаты кластеризации и визуализацию

## Структура проекта

```
company-clasterization/
├── __init__.py
├── config.ini               # Конфигурационный файл
├── dagster/                 # Dagster пайплайн
│   └── dagster-pipeline/
│       ├── dagster_pipeline/
│       │   ├── __init__.py
│       │   ├── assets.py    # Dagster ассеты
│       │   ├── definitions.py
│       │   ├── exps.ipynb   # Эксперименты
│       │   └── parse_lenta.py # Парсинг Lenta.ru
│       ├── dagster_pipeline_tests/
│       │   ├── __init__.py
│       │   └── test_assets.py
│       ├── data/
│       ├── pyproject.toml
│       └── README.md
├── data/                    # Данные
├── docker/                  # Docker конфигурация
├── docs/                    # Документация
│   ├── Architecture.drawio
│   ├── Architecture.png
│   ├── Block-scheme.png
│   ├── current_architecture.png
│   ├── DCMML_hw_3.md
│   ├── Global_Asset_Lineage.svg
│   └── ml_system_design_doc.md  # Дизайн документ
├── examples/                # Примеры использования
│   ├── cv_coherence_example.py
│   └── topic_evaluation_example.py
├── models/                  # Сохраненные модели
│   ├── bertopic_model
│   ├── bpe_model.vocab.model
│   └── bpe_model.vocab.vocab
├── notebooks/               # Jupyter ноутбуки
│   ├── DCMML_hw_2.ipynb
│   └── topics.ipynb
├── src/                     # Основной код
│   ├── __init__.py
│   ├── app.py               # Основной файл FastAPI приложения
│   ├── data/                # Модули для работы с данными
│   ├── database/            # Работа с базой данных
│   │   ├── __init__.py
│   │   └── database.py
│   ├── frontend/            # Streamlit приложение
│   │   └── app.py
│   ├── ml_core/             # Модули для работы с ML-моделями
│   │   ├── __init__.py
│   │   ├── cluster_plot.py  # Визуализация кластеров
│   │   ├── models.py        # Классы моделей
│   │   ├── pipeline.py      # ML-пайплайн
│   │   ├── topic_evaluation.py
│   │   └── utils.py
│   ├── models/              # Модели данных (Pydantic)
│   │   └── models.py
│   ├── utils/               # Вспомогательные функции
│   │   ├── __init__.py
│   │   └── auth.py          # Аутентификация
│   ├── test.ipynb
│   └── utils_.py
├── tests/                   # Тесты
├── LICENSE
├── pdm.lock
├── pyproject.toml
├── README.md                # Документация
├── requirements.txt         # Зависимости проекта
├── ruff.toml               # Конфигурация линтера
├── run.py                  # Скрипт для запуска
├── setup.py
└── .env                    # Переменные окружения
```

## Дополнительная информация

Система использует следующие ML-компоненты:
- **Sentence Transformers** для создания эмбеддингов текста
- **UMAP** для снижения размерности
- **HDBSCAN** для кластеризации
- **KeyBERT** для генерации информативных названий кластеров

# Company-clastering
Кластеризация компаний по новостям

Бизнес-идея:
Автоматическое объединение компаний в кластеры на основе новостных данных. Система анализирует новостные источники, выявляет упоминания компаний и группирует их по контекстуальной близости. Каждому кластеру присваивается информативное название, отражающее общую тематику. Пользователи могут выбирать период новостного охвата для анализа (день, неделя, месяц или произвольный период). Решение помогает бизнес-аналитикам быстро выявлять рыночные тренды, отслеживать конкурентов и находить потенциальных партнеров в схожих сферах деятельности.


```

├───data                <- Каталог со всеми данными
│   ├───external        <- Данные с "внешних" сервисов
│   ├───interim         <- Данные с "внутренних" сервисов
│   ├───processed       <- Конечные обработанные данные
│   └───raw             <- Исходные данные
├───docs                <- Документация
├───notebooks           <- Ноутбуки ipynb с экспериментами
├───src                 <- Основной код сервиса
│   └───data            <- Пакеты работы с данными
```
<<<<<<< HEAD
=======
---
# Дата проект

**См. каталог** `dagster\dagster-pipeline` - там весь пайплайн Dagster

## В ходе работы были **решены** следующие задачи:
- [X] Реализован `asset` для парсинга данных из сайта Lenta-ru  с последующим сохранением в csv-формат
- [X] Реализован `asset` для удаления пропусков в поле `text`
- [X] Реализован `asset` для полуения эмбеддингов текстов размера 512 с помощью мильтиязычной модели
- [X] Реализован `asset` для кластеризации текстов
- [X] Реализован `asset` для визуализации кластеров с использованием алгоритма TSNE на 3D


## Pipeline

![Pipeline](docs/Global_Asset_Lineage.svg)

## Что можно улучшить:
- [ ] Сделать отдельные ветки для сохранения результатов и для визуализации
- [ ] Использовать "событие" для отрисовки визуализации
- [ ] Созадть схему хранения данных в ретроспективе
- [ ] Больше логирования
- [ ] Улучшить ML-методы представления текстов
- [ ] По "умному" называть кластера
- [ ] Сделать алгоритм поиска количества кластеров динамическим
>>>>>>> origin/dev-Topic
