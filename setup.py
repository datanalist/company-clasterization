from setuptools import setup, find_packages

setup(
    name="company-clasterization",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # здесь можно указать зависимости из requirements.txt
    ],
)
